<label for="<%= index %>">
  <%# DIV %>
  <div class="ride-info d-flex align-items-center bg-whitesmoke" data-rides-target="infobox" data-action="click->rides#updateBackground" >
    <div class="d-flex align-items-center">
      <%= image_tag ride.path_image, alt: "Logo", class: "ride-logos" %>
      <h3> <%= ride.platform.name.capitalize %> </h3>
    </div>
    <div class="d-flex align-items-center">
      <p><%= ride.ETA %> min</p>
      <p>$ <%= ride.fare %></p>
    </div>
  </div>
  <input type="radio" name="ride" id="<%= index %>" class="hide-me">
</label>
